import ui.UIMainWindow;
import utils.Config;

class yllmc {
    public static void main(String[] args) {
        Config config = new Config();
        new UIMainWindow(
            config
        );
    }
}
